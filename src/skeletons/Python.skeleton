assignment: "%name% = %value%"
if: "if (%condition%):\n      %body%"
for: "for %var_name% in range(%start%, %end%):\n    %body%"
while: "while %condition%:\n      %body%"
print: "print(%value%)"
comment: "# %text%"
